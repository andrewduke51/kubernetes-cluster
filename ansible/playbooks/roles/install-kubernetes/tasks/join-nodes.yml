- debug:
    msg:  "{{ master }}"
  run_once: yes

#- name: Ansible insert a line before example
#  lineinfile:
#    path: /etc/hosts
#    line: '0.0.0.0  kube-api.local'
#  when: nodes == "yes"